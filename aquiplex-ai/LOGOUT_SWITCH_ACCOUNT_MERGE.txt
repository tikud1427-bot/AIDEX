AQUIPLEX safe logout/switch-account merge

Source feature archive:
  aquiplex-ai-account-menu.tar.gz
Target/current archive:
  aquiplex-ai.tar.gz

Restored from the feature archive:
  aqua-frontend/src/components/sidebar/AccountMenu.tsx
  aqua-frontend/src/stores/sessionStore.ts
  aqua-frontend/src/test/accountMenu.test.tsx

Important:
  These three files are content-identical to the current archive after CRLF/LF normalization.
  The current archive already contains the required logoutSession() and clearPersistedAppData()
  exports in aqua-frontend/src/api/account.ts and POST /api/account/logout in
  routes/account/account.routes.js, so those files were deliberately NOT overwritten.

This is intentionally a minimal/safe merge. No unrelated current code was replaced.
