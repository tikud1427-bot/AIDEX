<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Package visibility (Android 11+): lets the WebView hand off genuinely-external
         links (mailto, tel, and browser fallbacks) to other installed apps. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:theme="@style/Theme.App.Starting"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Aqua AI"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!--
            launchMode="singleTask": Chrome dispatches the App Link with FLAG_ACTIVITY_NEW_TASK.
            Under the default "standard" mode that can create a *second* MainActivity in a new
            task, which reloads AQUA_URL and discards the callback. singleTask guarantees one
            instance and delivers the callback through onNewIntent(). Its usual cost — clearing
            activities stacked above it — does not apply: this app has exactly one activity.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                OAuth return hop. Verified HTTPS App Link, not a custom scheme: a custom scheme
                is first-come-first-served on Android and any installed app can claim it to
                intercept the response (RFC 8252 section 8.1). Exact android:path, never
                pathPrefix="/" — the app must not claim the whole site and pull ordinary
                aquiplex.com links out of the browser device-wide.

                Requires https://aquiplex.com/.well-known/assetlinks.json to validate, or
                Android silently leaves the link with the browser. See docs/OAUTH_NATIVE_HANDOFF.md.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="aquiplex.com"
                    android:path="/auth/native/return" />
            </intent-filter>

        </activity>

    </application>

</manifest>
